{
  "name": "sonna3-app",
  "version": "1.0.0",
  "private": true,
  "description": "Sonnaع — native app shell (Capacitor) around the web marketplace.",
  "scripts": {
    "build:www": "node scripts/copy-web.mjs",
    "sync": "npm run build:www && cap sync",
    "sync:android": "npm run build:www && cap sync android",
    "sync:ios": "npm run build:www && cap sync ios"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/app": "^6.0.2",
    "@capacitor/core": "^6.2.0",
    "@capacitor/ios": "^6.2.0",
    "@capacitor/push-notifications": "^6.0.4",
    "@capacitor/splash-screen": "^6.0.3",
    "@capacitor/status-bar": "^6.0.2"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^6.2.0"
  }
}
